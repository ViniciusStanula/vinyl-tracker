# ============================================================
# robots.txt — garimpavinil.com.br
# ============================================================

User-agent: *
Allow: /
# Internal search results — noindex'd in metadata too (belt-and-suspenders)
Disallow: /*?*q=
Disallow: /*_rsc=
# User-specific / transactional pages. Scoped to the sub-routes: /alertas itself
# is a public signup page with no account data, and is indexable on purpose.
Disallow: /alertas/gerenciar
Disallow: /alertas/confirmar
# AI-training opt-out (contentsignals.org; advisory, not yet enforced)
Content-Signal: search=yes, ai-input=yes, ai-train=no

Sitemap: https://www.garimpavinil.com.br/sitemap.xml

# --- Common Crawl — block entirely --------------------------
# Primary purpose is AI training datasets; ai-train=no isn't honored by CCBot,
# so the only effective opt-out is Disallow.
#
# Other AI training crawlers (meta-externalagent, ClaudeBot, GPTBot, Bytespider)
# are deliberately NOT blocked, despite being 58% of bot traffic in bot_hits
# and a real share of the ISR/transfer bill. Presence in training data is what
# makes a model name this site unprompted, and that visibility is worth more
# here than the hosting it costs. Revisit only if the bill forces it.
User-agent: CCBot
Disallow: /

# --- SEO-tool crawlers — block entirely ---------------------
# Backlink/competitor scrapers that out-crawl Googlebot ~3:1 while providing
# zero value to us (they feed competitors' tools) and cost serverless
# invocations. All four honor robots.txt.
User-agent: SemrushBot
User-agent: AhrefsBot
User-agent: MJ12bot
User-agent: DotBot
Disallow: /
